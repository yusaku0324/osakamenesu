# Copy to .env.prod (gitignored) and fill with project-specific values before running
#   CLOUDSDK_CONFIG=/path/to/config ./scripts/deploy_api.sh --env-file .env.prod --rotate

# Required identifiers
PROJECT=
REGION=asia-northeast1
SERVICE=
MEILI_SERVICE=

# Runtime configuration
SITE_BASE_URL=
MEILI_HOST=
NOTIFY_SMTP_HOST=
NOTIFY_SMTP_PORT=2525
NOTIFY_SMTP_USERNAME=
NOTIFY_SMTP_PASSWORD=
NOTIFY_FROM_EMAIL=

# Magic link debugging must stay off in production. Toggle only for troubleshooting.
AUTH_MAGIC_LINK_DEBUG=false

# Optional overrides
# IMAGE=
# MEILI_MASTER_KEY=
# DATABASE_URL=
